<%= form_for @song, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :artist_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :artist_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :album_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :album_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :playlist_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :playlist_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                songs_path, :class => 'btn' %>
  </div>
<% end %>
